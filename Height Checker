class Solution:
    def heightChecker(self, heights: List[int]) -> int:
        new_res = sorted(heights)
        res = 0
        for i in range(len(heights)):
            if heights[i] != new_res[i]:
                res += 1
        return res
                
                
        
