class Solution:
    def isPowerOfTwo(self, n: int) -> bool:
        power = 0
        while True:
            if 2**power == n:
                return True
            if 2**power > n:
                return False
            power += 1
