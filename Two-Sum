class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for n in range(len(nums)):
            num = target - nums[n]
            nums[n] = ''
            if num in nums:
                return [n, nums.index(num)]

Dang... Tried a faster route...
