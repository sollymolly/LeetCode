class Solution:
    def sortedSquares(self, A: List[int]) -> List[int]:
        res = []
        for i in range(len(A)):
            res.append(A[i] ** 2)
        res = sorted(res)
        return res
