class Solution:
    def addBinary(self, a: str, b: str) -> str:
        a_change = int(a, 2)
        b_change = int(b, 2)
        ans = a_change + b_change
        return bin(ans).replace("0b", "")
        
